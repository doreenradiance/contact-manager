const initialState ={
     user : []
}

const userReducer = (state = initialState,action) => {
    switch (action.type){
        case"ADD_USER":
        const newUser=action.payload
       return {...state,contact:[...state.contact,newUser]};
       default:
       return state;
     }
}

export default contactReducer;